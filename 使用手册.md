# 链路状态路由协议分布式仿真系统 — 使用手册

---

> **适用人群：** 计算机网络课程学生、授课教师、项目后续维护开发者
>
> **项目地址：** https://github.com/nurzat0300/lsa

---

## 目录

- [1. 系统简介](#1-系统简介)
- [2. 环境要求](#2-环境要求)
- [3. 安装与启动](#3-安装与启动)
- [4. 界面操作指南](#4-界面操作指南)
- [5. 实验操作示例](#5-实验操作示例)
- [6. 如何添加新算法](#6-如何添加新算法)
- [7. 配置文件说明](#7-配置文件说明)
- [8. 命令行模式](#8-命令行模式)
- [9. 常见问题](#9-常见问题)
- [10. 项目结构](#10-项目结构)
- [11. 参考资料](#11-参考资料)

---

## 1. 系统简介

本系统是一个**链路状态路由协议（Link State Routing Protocol）的分布式仿真平台**，面向计算机网络课程教学。系统以 Python 实现，通过 PyQt5 提供图形界面，完整模拟了 OSPF（Open Shortest Path First）协议的核心机制。

### 1.1 模拟的协议机制

| 机制 | 真实 OSPF | 本系统实现 |
|------|----------|-----------|
| 邻居发现 | Hello 协议 | UDP 心跳消息，可配置间隔 |
| 链路状态通告 | LSA（Link State Advertisement） | 包含路由器 ID + 序列号 + 邻居列表 |
| 可靠洪泛 | 序列号去重 + 逐跳转发 | TTL 控制 + 重复检测 |
| 拓扑数据库 | LSDB（Link State Database） | 每路由器独立维护 NetworkX 图 |
| 最短路径计算 | Dijkstra SPF | 8 种算法可选，含分步动画 |
| 路由表生成 | 提取下一跳 | 完整路径 + 跳数统计 |
| 故障恢复 | 心跳超时 → 重新收敛 | 自动检测链路 Down / Up |

### 1.2 标签页一览

| 标签页 | 图标 | 用途 |
|--------|------|------|
| 教学演示 | 📖 | 8 步结构化教程，适合初学者入门 |
| 网络拓扑 | 🔍 | 实时拓扑图，支持视角切换和同步状态着色 |
| 算法动画 | 🎬 | Dijkstra 分步执行可视化，适合课堂投屏 |
| 路由表 | 📋 | 查看任意路由器的路由表和 LSDB 信息 |
| 算法对比 | 📊 | 8 种最短路径算法性能基准测试 |
| 仿真控制 | ⚙️ | 拓扑切换、故障注入、动态增删 |
| LSA 洪泛 | 📡 | LSA 事件追踪表格 + 逐跳传播动画 |
| 邻居状态 | 💓 | 邻居状态机 + Hello/Dead 定时器可视化 |

### 1.3 适用课程

- **计算机网络** — OSPF 路由协议原理、邻居状态机、LSA 洪泛机制
- **数据结构与算法** — 图论最短路径、Dijkstra 算法可视化
- **分布式系统** — 一致性、洪泛协议、故障检测与恢复

---

## 2. 环境要求

### 2.1 软硬件要求

| 项目 | 最低要求 | 推荐配置 |
|------|---------|----------|
| 操作系统 | Windows 10 / Ubuntu 20.04 / macOS 12 | Windows 10/11 |
| Python | 3.8 | **3.10**（3.12+ 也可运行） |
| 内存 | 2 GB | 4 GB+ |
| 磁盘空间 | 200 MB | 500 MB+ |
| 网络 | 无需联网（本地回环） | — |

### 2.2 Python 依赖

| 包 | 版本 | 用途 |
|----|------|------|
| `PyQt5` | ≥ 5.15 | 图形界面框架 |
| `PyQt5-sip` | ≥ 12.13 | PyQt5 辅助 |
| `numpy` | ≥ 1.24 | 数值计算 |
| `networkx` | ≥ 3.1 | 图数据结构 |
| `matplotlib` | ≥ 3.7 | 拓扑图绘制 |
| `setuptools` | ≥ 65 | 包构建工具 |
| `wheel` | ≥ 0.38 | 预编译包支持 |

### 2.3 验证环境

安装完成后，在终端运行以下命令验证：

```bash
python -c "import PyQt5; import networkx; import numpy; import matplotlib; print('环境就绪！')"
```

如果输出「环境就绪！」说明所有依赖已正确安装。

---

## 3. 安装与启动

### 3.1 获取项目

**方式一：从 GitHub 克隆（推荐）**

```bash
git clone https://github.com/nurzat0300/lsa.git
cd lsa
```

**方式二：下载 ZIP 压缩包**

1. 浏览器打开 https://github.com/nurzat0300/lsa
2. 点击绿色「Code」按钮 → 「Download ZIP」
3. 解压到任意目录

> **注意：** 如果 GitHub 访问困难，可使用镜像站（如 kgithub.com），或从任课老师 / 师兄师姐处直接拷贝整个项目文件夹。

### 3.2 安装依赖

**方式一：一键安装（Windows，强烈推荐）**

双击项目根目录下的 **`ui启动器.bat`**。

该脚本会按顺序自动执行：
1. 检测 Python 环境
2. 创建虚拟环境 `.venv`
3. 安装 setuptools 和 wheel
4. 使用清华镜像安装所有依赖（自动切换备用镜像）
5. 验证依赖完整性
6. 启动仿真界面

全程无需手动操作，首次安装约需 2~5 分钟。

**方式二：命令行手动安装**

```powershell
# 1. 打开终端（PowerShell 或 CMD），进入项目目录
cd D:\lsa

# 2. 创建虚拟环境
python -m venv .venv

# 3. 激活虚拟环境
.venv\Scripts\activate

# 4. 安装依赖（国内用户请使用镜像）
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

> **macOS / Linux 用户：** 激活虚拟环境命令为 `source .venv/bin/activate`，其余步骤相同。

### 3.3 启动系统

```bash
# 图形界面模式
python run.py --ui

# 命令行模式（无 GUI，适合远程运行）
python run.py --config config/network_topology.json

# 使用自定义配置文件
python run.py --config config/my_topology.json --ui
```

### 3.4 首次启动后的预期状态

启动后约 3~10 秒，你应该看到：

- **顶部状态栏：** `当前已加载拓扑: 12节点 | 已启动路由器: 12/12 | 活跃链路: 20 | 系统运行中...`
- **网络拓扑标签页：** 12 个圆形节点和连接线，节点颜色从灰色逐步变为绿色
- **路由表标签页：** 选择 Router1，路由表条目从 0 逐渐增长到 11

这表示各路由器已完成 LSA 同步，系统进入稳定运行状态。

---

## 4. 界面操作指南

> 本章按 8 个标签页逐一说明。建议打开系统对照操作。

### 4.1 📖 教学演示

**定位：** 初学者的第一站。8 个步骤覆盖链路状态路由协议的全部核心知识。

**界面布局：**
- **左侧：** 8 个步骤按钮，点击切换
- **右侧：** 对应步骤的详细知识讲解（HTML 排版）

**8 个教学步骤详解：**

| 步骤 | 核心知识点 | 建议配合操作 |
|------|-----------|-------------|
| 1. 理解网络拓扑 | 链路状态 vs 距离矢量、LSDB 概念 | 切换到「网络拓扑」标签页观察 |
| 2. 邻居发现 | Hello 协议、心跳机制、邻居状态 | 切换到「邻居状态」标签页观察状态机 |
| 3. LSA 生成 | LSA 结构（路由器ID + 序列号 + 邻居列表） | 切换到「LSA洪泛」标签页观察 CREATED 事件 |
| 4. LSA 洪泛 | 可靠洪泛、序列号去重、TTL 机制 | 切换到「LSA洪泛」标签页播放传播动画 |
| 5. SPF 计算 | Dijkstra 算法（贪心 + 松弛） | 切换到「算法动画」标签页逐步播放 |
| 6. 路由表生成 | 最短路径 → 下一跳提取 | 切换到「路由表」标签页查看结果 |
| 7. 链路故障 | 心跳超时 → 重新收敛 | 在「仿真控制」中模拟链路故障 |
| 8. 算法对比 | 时间复杂度对比（8 种算法） | 在「算法对比」标签页运行基准测试 |

**操作方式：**
- 点击左侧步骤按钮直接跳转
- 底部「◀ 上一步 / 下一步 ▶」顺序浏览
- 建议按顺序走完 8 步，再针对感兴趣的部分深入实验

---

### 4.2 🔍 网络拓扑

**定位：** 实时显示当前网络拓扑结构，支持全局和单路由器视角。

**控制栏：**

| 控件 | 功能 |
|------|------|
| 视角下拉框 | 选择观察视角——「全局视图」或某个路由器视角 |
| 自动刷新 | 勾选后每 2 秒自动更新（答辩/演示时建议关闭） |
| 刷新拓扑 | 手动立即刷新 |
| 重置布局 | 重新计算节点位置 |

**节点颜色含义（单路由器视角）：**

| 颜色 | 含义 | 说明 |
|------|------|------|
| 🔴 红色 | 当前视角路由器 | 你正在以它为中心观察网络 |
| 🟢 绿色 | 已同步节点 | 该节点已被视角路由器发现并建立拓扑 |
| ⚪ 灰色 | 未同步节点 | 视角路由器尚未收到该节点的 LSA |

**全局视图：** 所有节点为 🔵 蓝色，边权标签用白色气泡显示。

**边上的数字：** 链路成本（cost），值越小表示该链路越优先。

**典型使用场景：**
- **启动阶段：** 选择 Router1 视角，观察灰色节点逐个变绿——直观感受 LSA 同步过程
- **故障实验：** 模拟链路故障后，观察拓扑图边线变化
- **大拓扑：** 切换到 50 节点，系统自动简化渲染（隐藏边权、缩小节点）

---

### 4.3 🎬 算法动画

**定位：** Dijkstra 算法的分步执行可视化。**课堂教学投屏首选。**

**操作步骤：**

1. **选择源路由器** — 下拉框选择计算起点
2. **（可选）选择目标路由器** — 用于最后高亮最短路径
3. **开始播放** — 点击「▶ 播放」自动播放，或手动「◀ 上一步」「下一步 ▶」逐帧查看
4. **调节速度** — 速度滑块：1 = 非常慢，10 = 极速
5. **查看结果** — 运行完毕后点击「高亮最短路径」

**节点颜色详解：**

| 颜色 | 算法含义 |
|------|---------|
| 🔴 红色 | 源节点（起点） |
| 🟠 橙色 | 当前正在处理的节点（刚从优先队列弹出） |
| 🟢 绿色 | 已永久标记——到该节点的最短路径已确定 |
| 🔵 蓝色 | 边界节点——在候选集中，距离暂定，可能被更新 |
| ⚪ 灰色 | 未访问——算法尚未触及该节点 |

**边样式详解：**

| 样式 | 含义 |
|------|------|
| 灰色实线 | 普通网络链路 |
| 红色虚线 + 箭头 | 正在执行"松弛"操作的边 |
| 绿色粗线 + 光晕 + 箭头 | 最终最短路径 |

**右侧面板：**
- **步骤详情：** 用自然语言描述当前步骤做了什么
- **节点距离表：** 各节点当前距离 + 状态（已访问/边界/未访问），颜色与图中一致

**课堂技巧：**
- 手动步进模式 + 每步暂停讲解，效果远超自动播放
- 切换不同源路由器，展示最短路径树以不同节点为根的变化
- 配合距离表中 ∞ → 具体数值的变化讲解"松弛"操作的含义

---

### 4.4 📋 路由表

**定位：** 查看每个路由器的路由表和拓扑数据库状态。

**表格列说明：**

| 列 | 含义 | 示例 |
|----|------|------|
| 目标 | 目的节点 ID | 5 |
| 下一跳 | 数据包将转发给哪个邻居 | 3 |
| 成本 | 到达目标的总链路成本 | 8 |
| 完整路径 | 从源到目标的完整节点序列 | 1 → 3 → 5 |
| 跳数 | 经过的路由器数量 | 2 |

**下方信息面板显示：**
- 邻接路由器列表（直连邻居的 ID）
- 已知拓扑节点数（LSDB 中已有多少个路由器的信息）
- LSA 序列号（当前已发出的 LSA 版本号）
- 已缓存 LSA 数目（收到了多少个不同路由器的 LSA）

**自动刷新：** 每 2 秒更新一次，数据随仿真推进而变化。

**典型使用场景：**
- 切换不同路由器，观察路由表差异（因为各路由器以自己为根计算 SPF）
- 故障实验前后对比路由表路径变化
- 观察启动后路由表条目的增长过程

---

### 4.5 📊 算法对比

**定位：** 对 8 种最短路径算法进行多轮基准测试和性能对比。

**操作步骤：**
1. 从下拉框选择**源路由器**
2. 设置**测试轮数**（建议 10~30，轮数越多统计越稳定）
3. 点击「执行对比计算」

> 计算在后台线程执行，不阻塞界面。结果出来后弹出提示。

**8 种对比算法：**

| 算法 | 类型 | 时间复杂度 | 来源/说明 |
|------|------|-----------|----------|
| **Dijkstra** | 贪心 + 二叉堆 | O(E log V) | 经典最短路径算法 |
| **新型算法(分层桶)** | 桶优化 | O(E log V) | 按距离分桶减少堆操作 |
| **Duan2025** | 递归分治 | O(m log²/³ n) | STOC 2025 最佳论文，突破排序障碍 |
| **Bellman-Ford** | 动态规划 | O(VE) | 支持负权边（本项目只用正权） |
| **SPFA** | 队列优化 | O(kE) | Bellman-Ford 工程优化版 |
| **Prim(MST基线)** | 最小生成树 | O(E log V) | **非最短路径**，用作对比参照 |
| **Floyd-Warshall** | 全源 DP | O(V³) | 同时计算所有节点对 |
| **A\* (多目标)** | 启发式搜索 | O(E) | 跳数启发式加速 |

**结果解读：**

| 指标 | 含义 |
|------|------|
| 平均时间(ms) | 各轮耗时的算术平均 |
| 最小/最大(ms) | 最快/最慢的一轮 |
| 平均访问节点 | 算法访问了多少个不同节点 |
| 平均迭代 | 主循环执行了多少轮 |
| 平均松弛 | 执行了多少次距离更新操作 |

**教学实验建议：**
- 分别在 8/20/50 节点拓扑下运行，对比时间增长趋势
- Floyd-Warshall 在 50 节点下 O(V³) 劣势极其明显
- Duan2025 在大规模下访问节点数接近理论最优

---

### 4.6 ⚙️ 仿真控制

**定位：** 控制系统运行状态，模拟网络事件。

**功能区说明：**

| 功能区 | 操作 | 效果 |
|--------|------|------|
| **拓扑切换** | 下拉选择（8/20/50节点）→ 点击「加载并演示」 | 一键更换拓扑（后台线程执行，不卡界面） |
| **链路故障** | 输入两个路由器 ID → 「创建故障」 | 链路断开，全网自动重新收敛 |
| **链路恢复** | 输入两个路由器 ID + 成本 → 「恢复链路」 | 链路重新建立，路由更新 |
| **新增路由器** | 输入 ID + 端口号 → 「添加路由器」 | 运行时动态加入新路由器 |
| **新增链路** | 输入两个 ID + 成本 → 「添加链路」 | 运行时动态添加链路 |

**下方统计面板：**
- 小规模（≤ 39 节点）：逐个路由器显示详细统计
- 大规模（≥ 40 节点）：自动切换汇总模式（平均 + 总数），避免卡顿

---

### 4.7 📡 LSA 洪泛

**定位：** 实时追踪每一条 LSA 从产生到消亡的完整生命周期。

**上半部分 — LSA 事件表格：**

| 列 | 说明 |
|----|------|
| 时间 | 精确到毫秒的事件时间戳 |
| 事件 | 事件类型（彩色标注） |
| LSA源 | LSA 最初由哪个路由器产生 |
| 序列号 | 该 LSA 的序列号 |
| 详情 | 人类可读描述（如 "Router3 洪泛LSA(from R1) → [5, 7]"） |

**事件类型与颜色：**

| 颜色 | 类型 | 含义 |
|------|------|------|
| 🟢 绿色 | 生成 CREATED | 路由器主动生成新 LSA（序列号+1） |
| 🔵 蓝色 | 接受新 RECEIVED_NEW | 收到更新的 LSA，已存入 LSDB |
| ⚪ 灰色 | 丢弃旧 RECEIVED_OLD | 收到的 LSA 序列号比已有更旧 |
| 🟠 橙色 | 丢弃重复 RECEIVED_DUP | 序列号相同，无需重复处理 |
| 🟣 紫色 | 洪泛转发 FORWARDED | LSA 已转发给所有邻居（除发送者） |
| 🔴 红色 | TTL耗尽 DISCARDED_TTL | 跳数减到 0，停止转发 |

**下半部分 — 洪泛拓扑动画：**

- 节点颜色实时反映 LSA 到达状态
- 紫色虚线 = LSA 传播经过的边
- 「▶ 自动播放」按时间顺序回放全部事件
- 点击表格任意行可跳到对应时刻的快照
- 速度滑块控制播放速度

**控制栏功能：**
- **过滤源：** 只看某个路由器产生的 LSA
- **自动滚动：** 表格自动跟随最新事件
- **清空日志：** 重置事件记录和动画

**教学用途：**
- 观察一条 LSA 如何在拓扑中逐跳扩散
- 理解序列号如何实现去重（为什么"丢弃重复"事件最多）
- 观察 TTL 机制如何防止无限洪泛

---

### 4.8 💓 邻居状态

**定位：** 可视化邻居状态机和 Hello/Dead 定时器。

**界面组成：**

- **路由器选择下拉框：** 选择观察视角（哪个路由器的邻居列表）
- **邻居卡片列表：** 可滚动，每个邻居一张卡片

**每张卡片包含：**

| 元素 | 说明 |
|------|------|
| 邻居名称 | Router ID + 路由器名称 |
| 当前状态标签 | 彩色显示 Down / Init / 2-Way / Full |
| 链路成本 | 到该邻居的链路开销 |
| 状态机进度条 | 四阶段可视化：`Down ████ Init ████ 2-Way ████ Full` |
| Hello 倒计时条 | 蓝色→绿色渐变，显示距上次收到 Hello 的时间 |
| Dead 超时条 | 黄色→红色渐变，显示心跳丢失计数进度 |

**邻居状态机模型：**

```
                    ┌──────────收到第一个Hello───────────┐
                    ▼                                      │
Down ──────────→ Init ──────────→ 2-Way ──────────→ Full
  ▲               ▲                   ▲                  │
  │               │                   │                  │
  └───超时────────┴────超时───────────┴────超时──────────┘
```

**状态含义：**

| 状态 | 颜色 | 含义 |
|------|------|------|
| Down | 🔴 红 | 邻居不可达，未收到任何心跳 |
| Init | 🟠 橙 | 收到第一个 Hello，正在初始化连接 |
| 2-Way | 🔵 蓝 | 双向通信已确认，开始交换 LSA |
| Full | 🟢 绿 | LSA 同步完成，路由已计算 |

**定时器解说：**
- **Hello 条（蓝→绿）：** 倒计时显示距上次收到 Hello 的时间。如果超过 Hello 间隔（默认 0.8s）未收到新 Hello，计数器开始增长
- **Dead 条（黄→红）：** 心跳丢失次数累计。超过阈值（默认 3 次）→ 邻居状态回退到 Down

**教学用途：**
- 在仿真控制中模拟链路故障，观察邻居状态从 Full → Down 的全过程
- 恢复链路后观察 Init → 2-Way → Full 的渐进恢复
- 理解 OSPF 邻居状态机的工程意义

---

## 5. 实验操作示例

### 实验一：观察 LSA 洪泛与路由收敛

**实验目的：** 理解链路状态路由协议启动后拓扑逐步同步的过程。

**建议时长：** 10 分钟

**操作步骤：**

1. 启动系统：`python run.py --ui`
2. 观察顶部状态栏，确认显示「已启动路由器: 12/12」
3. 切换到 **📡 LSA洪泛** 标签页
4. 观察事件表格中 CREATED → FORWARDED → RECEIVED_NEW 事件序列持续滚动
5. 点击「▶ 自动播放」，观察拓扑图中 LSA 如何从某个源头逐跳扩散到全网
6. 切换到 **📋 路由表** 标签页，选择 Router1
7. 观察路由表条目数从 0 逐渐增长到 11（耗时约 5~10 秒）
8. 切换到 **🔍 网络拓扑** 标签页，选择 Router1 视角
9. 观察节点逐个从灰色（未同步）变为绿色（已同步）

**预期结果：**
- 启动约 5~10 秒后，所有 12 个路由器的路由表完整（各有 11 条路由）
- LSA 事件中"丢弃重复"数量最多（因为同一个 LSA 可能通过多条路径到达）
- 拓扑图中所有节点最终变为绿色

**思考题：**
1. 为什么启动后不能立刻获得完整路由表？
2. 为什么「丢弃重复」事件占大多数？这说明了什么？

---

### 实验二：链路故障与重新收敛

**实验目的：** 观察链路故障后路由协议的自动恢复能力。

**建议时长：** 15 分钟

**操作步骤：**

1. 确保系统已稳定运行（所有路由器路由表完整）
2. 切换到 **📋 路由表**，选择 Router1，记录到 Router5 的如下信息：
   - 路径：`___ → ___ → ___`
   - 总成本：`___`
3. 切换到 **💓 邻居状态**，选择 Router1，观察所有邻居状态应为 Full
4. 切换到 **⚙️ 仿真控制**：
   - 路由器1 = `1`，路由器2 = `3`
   - 点击「创建故障」
5. 回到 **📋 路由表**，观察 Router1 到 Router5 的路径是否自动变化：
   - 新路径：`___ → ___ → ___`
   - 新成本：`___`
6. 回到 **💓 邻居状态**，观察 Router3（如果原本是 Router1 的邻居）的状态从 Full → Down
7. 回到 **⚙️ 仿真控制**，恢复链路：
   - 路由器1 = `1`，路由器2 = `3`，成本 = `8`
   - 点击「恢复链路」
8. 再次查看路由表和邻居状态

**预期结果：**
- 故障后，经过 Router3 的路径会被绕开，可能经过更多跳数
- 路径成本通常变大（绕路）
- 邻居状态从 Full → Down → Init → 2-Way → Full（约 3~5 秒完成）
- 恢复链路后，路径可能回到原始路径（取决于成本）

**思考题：**
1. 故障后路径发生了什么变化？成本为什么可能变大？
2. 从故障发生到路由表更新之间有多长延迟？这个延迟由什么决定？

---

### 实验三：算法动画课堂演示

**实验目的：** 直观展示 Dijkstra 算法的工作过程，适合课堂教学。

**建议时长：** 20 分钟（课堂）

**操作步骤：**

1. 切换到 **🎬 算法动画** 标签页
2. 源路由器选择 `Router1`，速度调为「慢速」
3. 点击「▶ 播放」
4. 逐帧观察以下变化：
   - 节点颜色：⚪ 灰色 → 🔵 蓝色（候选）→ 🟢 绿色（已确定）
   - 🔴 红色虚线 = 正在松弛的边
   - 右侧距离表数值逐步更新（∞ → 具体值）
5. 运行完毕后：
   - 目标路由器选择 `Router8`
   - 点击「高亮最短路径」
   - 绿色粗线显示 Router1 → Router8 的实际最短路径
6. **（可选）** 切换源路由器为 Router6，重新播放，观察最短路径树的变化

**课堂技巧：**
- 使用**手动步进模式**（「◀ 上一步」「下一步 ▶」），每步暂停讲解
- 聚焦「松弛」操作的步骤：观察目标距离从较大值变成更小值
- 提问学生：当前哪个节点会被下一次选中？为什么？

**思考题：**
1. 为什么 Dijkstra 算法保证找到的是全局最短路径？
2. 松弛操作在做什么？

---

### 实验四：不同规模下的算法性能对比

**实验目的：** 理解时间复杂度在实际运行中的表现。

**建议时长：** 25 分钟

**操作步骤：**

1. **⚙️ 仿真控制** → 选择「小型拓扑（8节点）」→ 加载并演示 → 等待系统稳定
2. **📊 算法对比** → 源路由器选择 Router1，轮数设为 `30`
3. 点击「执行对比计算」→ 等待完成 → 记录下表：

| 算法 | 8节点平均耗时 | 20节点平均耗时 | 50节点平均耗时 |
|------|:-----------:|:-----------:|:-----------:|
| Dijkstra | | | |
| 新型算法 | | | |
| Duan2025 | | | |
| Bellman-Ford | | | |
| SPFA | | | |
| Prim(MST) | | | |
| Floyd-Warshall | | | |
| A* | | | |

4. 依次切换到 20 节点和 50 节点拓扑，重复步骤 2~3
5. 对比三种规模下的耗时增长趋势

**预期观察：**

- Floyd-Warshall 在 50 节点下 O(V³) 劣势极其明显（可能从 <1ms 飙升至数十 ms）
- Dijkstra / 桶算法 / Duan2025 耗时增长平缓
- A* 在大规模下访问节点数显著少于其他算法
- Bellman-Ford 在大规模下迭代次数最多

**思考题：**
1. 为什么 Floyd-Warshall 在小规模时也很快，但大规模时突然变慢？
2. Duan2025 论文号称"突破排序障碍"，从数据上看它在这个规模上有优势吗？

---

### 实验五：自定义网络拓扑

**实验目的：** 学习配置文件格式，创建自己的拓扑进行实验。

**建议时长：** 15 分钟

**操作步骤：**

1. 复制模板文件：
   ```bash
   copy config\test_topology1.json config\my_topology.json
   ```
2. 用文本编辑器（记事本 / VS Code）打开 `config\my_topology.json`
3. 参考第 7 章格式，编辑自己的路由器和链路
4. 启动自定义拓扑：
   ```bash
   python run.py --config config\my_topology.json --ui
   ```
5. 验证拓扑连通性：切换到「网络拓扑」确认所有节点可达

**拓扑设计要求：**
- 必须是**连通图**（任意两点间有路径）
- 端口号不冲突（建议从 21001 递增）
- 链路成本 ≥ 1

---

## 6. 如何添加新算法

> 系统采用插件式架构，添加新算法只需修改 **3 个文件**，遵循标准接口即可。

### 6.1 接口规范

所有算法必须实现以下两个方法：

```python
def calculate_shortest_paths(
    self,
    source: int,
    graph: Dict[int, List[Tuple[int, int]]],
) -> Dict[int, Tuple[int, List[int]]]:
    """
    参数:
        source: 源节点 ID（整数，例如 1）
        graph:  邻接表 {节点ID: [(邻居ID, 边权), ...]}
                例如 {1: [(2, 4), (3, 8)], 2: [(1, 4), (3, 2)], ...}

    返回:
        {目标节点ID: (总成本, [完整路径列表]), ...}
        例如 {2: (4, [1, 2]), 3: (6, [1, 2, 3]), ...}
        注意：不包含 source → source（自身）
    """

def get_statistics(self) -> Dict:
    """
    返回:
        {
            'algorithm_name':    str,    # 算法名称
            'computation_time':  float,  # 耗时（毫秒）
            'visited_nodes':     int,    # 访问节点数
            'iterations':        int,    # 主循环迭代次数
            'relaxation_count':  int,    # (可选) 松弛操作次数
        }
    """
```

### 6.2 完整示例：以双向 Dijkstra 为例

#### 第一步：编写算法类

在 `src/algorithms/` 下新建文件 `bidirectional_dijkstra.py`：

```python
"""
双向Dijkstra算法 — 从起点和终点同时向中间搜索
"""
import heapq
import time
from typing import Dict, List, Tuple


class BidirectionalDijkstra:

    def __init__(self):
        self.computation_time = 0.0
        self.visited_nodes = 0
        self.iterations = 0

    def calculate_shortest_paths(self, source, graph):
        start_time = time.perf_counter()
        self.visited_nodes = 0
        self.iterations = 0

        # ==== 你的算法实现 ====
        distances = {source: 0}
        paths = {source: [source]}
        visited = set()
        pq = [(0, source)]

        while pq:
            self.iterations += 1
            curr_cost, curr_node = heapq.heappop(pq)
            if curr_node in visited:
                continue
            visited.add(curr_node)
            self.visited_nodes += 1

            if curr_node in graph:
                for neighbor, cost in graph[curr_node]:
                    new_cost = curr_cost + cost
                    if neighbor not in distances or new_cost < distances[neighbor]:
                        distances[neighbor] = new_cost
                        paths[neighbor] = paths[curr_node] + [neighbor]
                        heapq.heappush(pq, (new_cost, neighbor))

        self.computation_time = time.perf_counter() - start_time

        result = {}
        for dest in distances:
            if dest != source:
                result[dest] = (distances[dest], paths.get(dest, []))
        return result

    def get_statistics(self):
        return {
            'algorithm_name': 'BidirectionalDijkstra',
            'computation_time': self.computation_time * 1000,
            'visited_nodes': self.visited_nodes,
            'iterations': self.iterations,
        }
```

#### 第二步：在三个位置注册

**位置 1 — `src/algorithms/__init__.py`（末尾追加）：**

```python
from .bidirectional_dijkstra import BidirectionalDijkstra   # ← 新增导入

__all__ = [
    'DijkstraAlgorithm',
    'NewAlgorithm',
    'DuanAlgorithm',
    # ... 已有的 ...
    'BidirectionalDijkstra',     # ← 新增导出
]
```

**位置 2 — `src/core/protocol.py`（三处修改）：**

```python
# ① 文件顶部添加导入
from ..algorithms.bidirectional_dijkstra import BidirectionalDijkstra

class PathCalculator:
    def __init__(self, ...):
        # ② 实例化
        self.bidir_algo = BidirectionalDijkstra()

        # ③ 注册到 registry（约在第 36 行）
        self.algorithm_registry = {
            'dijkstra':       ('Dijkstra',            self.dijkstra_algo),
            # ... 已有的 ...
            'bidirectional':  ('BidirectionalDijkstra', self.bidir_algo),  # ← 新增
        }
```

**位置 3 — `src/ui/main_window.py`（两处修改）：**

```python
# ① 在 AlgorithmComparisonWidget.__init__ 中，约第 176 行：
self.algorithm_display_map = {
    'dijkstra':       'Dijkstra算法',
    # ... 已有的 ...
    'bidirectional':  '双向Dijkstra',    # ← 新增
}

# ② 在 _on_benchmark_finished 方法中，约第 269 行：
ordered_keys = [
    'dijkstra', 'new_algo', 'duan', 'bellman_ford',
    'spfa', 'prim_mst', 'floyd_warshall', 'astar',
    'bidirectional',    # ← 新增
]
```

#### 第三步：验证

1. 启动系统：`python run.py --ui`
2. 切换到 **📊 算法对比** 标签页
3. 选择任意路由器 → 轮数设为 5 → 点击「执行对比计算」
4. 确认「双向Dijkstra」出现在结果表格中，且耗时、访问节点数等数据合理

### 6.3 添加算法检查清单

- [ ] 新建 `src/algorithms/xxx.py`，实现两个接口方法
- [ ] `src/algorithms/__init__.py` — 添加 `from .xxx import ...`
- [ ] `src/algorithms/__init__.py` — `__all__` 中添加导出
- [ ] `src/core/protocol.py` — 顶部添加 `from ..algorithms.xxx import ...`
- [ ] `src/core/protocol.py` — `__init__` 中实例化
- [ ] `src/core/protocol.py` — `algorithm_registry` 中注册
- [ ] `src/ui/main_window.py` — `algorithm_display_map` 添加中文名
- [ ] `src/ui/main_window.py` — `ordered_keys` 添加排序键
- [ ] 启动系统验证

---

## 7. 配置文件说明

### 7.1 配置文件结构

拓扑配置文件为 JSON 格式，位于 `config/` 目录。

```json
{
  "routers": [
    {
      "router_id": 1,
      "router_name": "Router1",
      "listen_port": 21001,
      "listen_ip": "127.0.0.1"
    },
    {
      "router_id": 2,
      "router_name": "Router2",
      "listen_port": 21002,
      "listen_ip": "127.0.0.1"
    }
  ],
  "links": [
    {
      "router1_id": 1,
      "router2_id": 2,
      "cost": 4
    }
  ]
}
```

### 7.2 字段说明

**routers 数组中每个对象：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `router_id` | int | 是 | 路由器唯一标识（1 ~ 255） |
| `router_name` | string | 是 | 显示名称，如 `"Router1"` |
| `listen_port` | int | 是 | UDP 监听端口（10000 ~ 65535），不可重复 |
| `listen_ip` | string | 否 | 本地用 `"127.0.0.1"`，分布式部署改为实际 IP |

**links 数组中每个对象：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `router1_id` | int | 是 | 链路一端 |
| `router2_id` | int | 是 | 链路另一端 |
| `cost` | int | 是 | 链路成本（1 ~ 65535），越小越优先 |

### 7.3 现有拓扑文件

| 文件 | 节点数 | 链路数 | 适用场景 |
|------|:----:|:----:|---------|
| `topology_small_demo.json` | 8 | 13 | 快速演示、课堂教学 |
| `network_topology.json` | 12 | 20 | **默认拓扑**、日常使用 |
| `topology_medium_demo.json` | 20 | 35 | 中型测试、算法对比 |
| `benchmark_topology_50.json` | 50 | 90 | 压力测试、性能基准 |
| `test_topology1.json` | 5 | 7 | 单元测试 |
| `test_topology2.json` | 5 | 7 | 故障恢复测试 |
| `test_topology3.json` | 4 | 5 | 路由表验证 |

### 7.4 设计注意事项

1. **链路成本必须 ≥ 1。** 成本 0 会导致算法异常。
2. **拓扑必须是连通图。** 不连通会导致部分节点间无路由。
3. **端口不能冲突。** 建议从 21001 开始，每个路由器使用不同端口。
4. **50 节点及以上**，系统自动启用稳态参数：「心跳容忍度提升」「LSA 发送降频」「日志级别升为 WARNING」「故障检测静默期延长」。

---

## 8. 命令行模式

系统也支持无 GUI 的命令行模式，适合远程运行或自动化测试。

### 8.1 启动

```bash
python run.py --config config/network_topology.json
```

### 8.2 可用命令

启动后在 `>` 提示符下输入：

| 命令 | 说明 | 示例 |
|------|------|------|
| `summary` | 打印所有路由器的路由表摘要 | `summary` |
| `topology` | 以 JSON 格式输出全网拓扑 | `topology` |
| `routes <id>` | 查看指定路由器的路由表 | `routes 1` |
| `fail <a> <b>` | 模拟链路 a↔b 故障 | `fail 1 2` |
| `recover <a> <b> <c>` | 恢复链路 a↔b，成本 = c | `recover 1 2 10` |
| `help` | 显示帮助信息 | `help` |
| `quit` | 退出系统 | `quit` |

### 8.3 运行自动化测试

```bash
python tests/run_tests.py
```

---

## 9. 常见问题

### 安装类

**Q1：启动报错 "DLL load failed" 或 "找不到 Qt 平台插件"**

原因：PyQt5 安装不完整或版本与 Python 不兼容。

解决：
```bash
pip uninstall PyQt5 PyQt5-sip -y
pip install PyQt5 PyQt5-sip
```

**Q2：`ui启动器.bat` 运行时提示 "Cannot import setuptools.build_meta"**

原因：Python 3.12+ 的 venv 不再自带 setuptools，但旧版 numpy 需要编译安装。

解决：已修复，最新版 `ui启动器.bat` 会先安装 setuptools 和 wheel。如果仍有问题，手动运行：
```bash
.venv\Scripts\python.exe -m pip install --upgrade setuptools wheel
.venv\Scripts\python.exe -m pip install -r requirements.txt
```

**Q3：安装依赖时下载速度很慢**

原因：PyPI 官方服务器在海外。

解决：使用国内镜像：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

`ui启动器.bat` 已内置清华镜像，并会自动尝试阿里云、中科大备用镜像。

**Q4：`git clone` 速度慢或连不上**

解决：
- 使用代理或 VPN
- 使用 GitHub 镜像站（如 `kgithub.com`）：
  ```bash
  git clone https://kgithub.com/nurzat0300/lsa.git
  ```
- 从老师或同学处直接拷贝项目文件夹

### 运行类

**Q5：防火墙提示网络访问**

原因：路由器之间使用本地 UDP（127.0.0.1）通信。

解决：点击「允许访问」即可。本系统只在本地回环通信，不会连接外网。

**Q6：端口被占用（启动失败）**

原因：上次未正常退出，Python 进程仍占用端口。

**解决步骤：**
1. 打开任务管理器（Ctrl+Shift+Esc）
2. 在「详细信息」中找到所有 `python.exe` 进程
3. 选中 → 结束任务
4. 重新启动

**Q7：50 节点拓扑界面卡顿**

原因：大量路由表渲染开销。

解决（按需选择）：
- 减少同时打开的标签页数量
- 关闭「网络拓扑」标签页的自动刷新
- 编辑 `src/core/constants.py`，调大 `LSA_SEND_INTERVAL` 和 `HEARTBEAT_INTERVAL`

**Q8：LSA 洪泛标签页事件数量太多**

这是正常现象。每次 LSA 交换会产生多条事件（生成 + 转发 + 多个路由器的接收/重复检测）。12 节点默认拓扑稳定后约产生数百至上千条事件。

解决办法：
- 使用「过滤源」下拉框只看某个路由器的事件
- 点击「清空日志」重置

**Q9：如何修改心跳检测间隔？**

编辑 `src/core/constants.py`：
```python
HEARTBEAT_INTERVAL = 0.8    # 心跳发送间隔（秒），越小越快但 CPU 占用越高
HEARTBEAT_TIMEOUT = 3       # 连续丢失多少次心跳后判定邻居故障
```

修改后重启系统生效。

**Q10：如何在多台机器上分布式运行？**

1. 修改配置文件中的 `listen_ip` 为各机器的实际局域网 IP
2. 确保各机器网络互通（能 ping 通）
3. 防火墙放行配置中使用的 UDP 端口
4. 将同一份配置文件复制到所有机器
5. 各机器分别执行 `python run.py --config <配置路径>`

---

## 10. 项目结构

```
lsa/
│
├── run.py                      # 主入口：python run.py --ui
├── requirements.txt            # Python 依赖清单
├── ui启动器.bat                 # 【推荐】一键安装 + 启动
├── .gitignore                  # Git 忽略规则
├── 使用手册.md                  # 【本手册】
│
├── config/                     # 拓扑配置文件（JSON）
│   ├── network_topology.json   # 默认 12 节点
│   ├── topology_small_demo.json # 8 节点演示
│   ├── topology_medium_demo.json # 20 节点中型
│   ├── benchmark_topology_50.json # 50 节点压力
│   └── test_topology*.json     # 自动化测试用
│
├── src/                        # 源代码
│   ├── simulator.py            # 仿真主控：路由器管理、故障注入、拓扑切换
│   │
│   ├── algorithms/             # ★ 算法模块 — 添加新算法在这里
│   │   ├── __init__.py         #   【算法注册位置 1】导入 + 导出
│   │   ├── dijkstra.py         #   经典 Dijkstra（二叉堆）
│   │   ├── new_algorithm.py    #   分层桶优化算法
│   │   ├── duan_algorithm.py   #   Duan et al. STOC 2025 最佳论文
│   │   ├── extended_algorithms.py # Bellman-Ford / SPFA / Prim / Floyd / A*
│   │   └── stepped_dijkstra.py #   分步执行版（算法动画用）
│   │
│   ├── core/                   # 协议引擎 + 数据结构
│   │   ├── constants.py        #   枚举、配置常量、数据类
│   │   ├── lsa.py              #   LSA 数据包 + LSAEventLog（全局事件追踪）
│   │   ├── protocol.py         #   【算法注册位置 2】PathCalculator
│   │   ├── router.py           #   路由器核心：6 线程 + UDP + LSA 处理
│   │   └── topology.py         #   TopologyDB + RoutingTable
│   │
│   └── ui/                     # 图形界面（PyQt5）
│       ├── __init__.py
│       ├── main_window.py      #   【算法注册位置 3】主窗口 + 显示名
│       ├── styles.py           #   全局 QSS 样式表
│       ├── teaching_demo.py    #   教学演示（8 步教程）
│       ├── topology_view.py    #   网络拓扑可视化（matplotlib）
│       ├── animation_view.py   #   Dijkstra 算法动画（QPainter）
│       ├── lsa_monitor.py      #   LSA 洪泛追踪 + 传播动画
│       └── neighbor_monitor.py #   邻居状态机 + Hello/Dead 定时器
│
├── docs/                       # 开发者文档
│   ├── TECHNICAL.md
│   └── USAGE.md
│
└── tests/                      # 自动化测试
    └── run_tests.py
```

### 系统架构图

```
┌──────────────────────────────────────────────────────────┐
│                 UI 层 (PyQt5) — 8 个标签页                 │
│                                                            │
│  📖教学演示  🔍网络拓扑  🎬算法动画  📋路由表              │
│  📊算法对比  ⚙️仿真控制  📡LSA洪泛  💓邻居状态             │
└────────────────────────┬─────────────────────────────────┘
                         │ 方法调用
┌────────────────────────▼─────────────────────────────────┐
│              仿真系统层 (RoutingSimulator)                 │
│                                                            │
│  load_config() → build_network() → start_simulation()     │
│  switch_topology_profile() ← 拓扑切换                     │
│  simulate_link_failure()   ← 故障注入                     │
│  get_topology() / get_routing_table() ← UI 数据查询        │
└────────────────────────┬─────────────────────────────────┘
                         │ 管理 N 个实例
┌────────────────────────▼─────────────────────────────────┐
│                 路由器层 (Router × N)                      │
│                                                            │
│  每个路由器 6 个工作线程：                                  │
│  ┌──────────┬──────────┬──────────┬──────────┐            │
│  │ Recv线程  │ Send线程  │Process线程│ LSA线程  │            │
│  │ 接收UDP   │ 发送UDP   │ 处理消息  │ 定时LSA  │            │
│  └──────────┴──────────┴──────────┴──────────┘            │
│  ┌──────────┬──────────┐                                  │
│  │Heartbeat │ SPF线程   │                                  │
│  │ 心跳检测  │ 节流计算  │                                  │
│  └──────────┴──────────┘                                  │
│                                                            │
│  UDP 通信：127.0.0.1:21001 ~ 21050                        │
│  消息类型：HEARTBEAT / HELLO / LSA / ACK                   │
└────────────────────────┬─────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────┐
│                   协议与算法层                              │
│                                                            │
│  PathCalculator ← 8 种最短路径算法                         │
│  TopologyDB     ← NetworkX 图 + 邻接表                    │
│  RoutingTable   ← {目标: (下一跳, 成本, 完整路径)}         │
│  LSADatabase    ← {源路由器: (序列号, 时间戳, LSA数据)}    │
│  LSAEventLog    ← 全局事件总线（跨路由器共享）              │
└──────────────────────────────────────────────────────────┘
```

---

## 11. 参考资料

- Dijkstra, E. W. (1959). *A note on two problems in connexion with graphs.* Numerische Mathematik, 1, 269–271.
- RFC 2328 — *OSPF Version 2* (1998). IETF.
- Duan, Mao, Mao, Shu, Yin (2025). *Breaking the Sorting Barrier for Directed Single-Source Shortest Paths.* STOC 2025.
- Kurose & Ross. *Computer Networking: A Top-Down Approach* (8th Edition). Pearson.
- NetworkX 文档：https://networkx.org/
- PyQt5 文档：https://www.riverbankcomputing.com/static/Docs/PyQt5/

---

> *本手册面向计算机网络课程的学生、授课教师以及后续维护该项目的开发者。*
>
> *发现问题或建议改进？欢迎提 Issue 或 Pull Request：https://github.com/nurzat0300/lsa*
