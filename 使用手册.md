# 链路状态路由协议分布式仿真系统 — 使用手册

---

## 目录

1. [系统简介](#1-系统简介)
2. [环境要求](#2-环境要求)
3. [安装与启动](#3-安装与启动)
4. [界面操作指南](#4-界面操作指南)
   - 4.1 教学演示
   - 4.2 网络拓扑
   - 4.3 算法动画
   - 4.4 路由表
   - 4.5 算法对比
   - 4.6 仿真控制
   - 4.7 LSA 洪泛监控
   - 4.8 邻居状态机
5. [实验操作示例](#5-实验操作示例)
6. [如何添加新算法](#6-如何添加新算法)
7. [配置文件说明](#7-配置文件说明)
8. [常见问题](#8-常见问题)
9. [项目结构](#9-项目结构)

---

## 1. 系统简介

本系统是一个**链路状态路由协议（Link State Routing Protocol）的分布式仿真平台**，用于计算机网络课程教学。系统模拟了 OSPF 协议的核心机制，包括邻居发现、LSA 洪泛、SPF 最短路径计算和路由表生成。

### 核心功能一览

| 功能模块 | 说明 |
|---|---|
| 分布式路由器仿真 | 每个路由器独立运行，通过 UDP 进行通信 |
| Hello 协议 | 心跳检测与邻居发现，可视化状态机 |
| LSA 洪泛 | 可靠的链路状态通告传播，含实时动画追踪 |
| SPF 计算 | 基于 Dijkstra 算法的最短路径优先计算 |
| 算法动画 | Dijkstra 算法的分步执行可视化 |
| 多算法对比 | 集成 **8 种**最短路径算法的性能对比 |
| 故障模拟 | 链路故障与恢复的动态演示 |
| 拓扑切换 | 支持 8 / 12 / 20 / 50 节点一键切换 |

### 适用课程

- **计算机网络** — 路由协议章节（OSPF 原理教学）
- **数据结构与算法** — 图论最短路径（Dijkstra 可视化）
- **分布式系统** — 一致性、洪泛机制、故障恢复

---

## 2. 环境要求

| 项目 | 最低要求 | 推荐配置 |
|---|---|---|
| 操作系统 | Windows 10 / Linux / macOS | Windows 10+ |
| Python | 3.8 ~ 3.11 | **3.10** |
| 内存 | ≥ 2 GB | 4 GB |
| 磁盘 | ≥ 500 MB | 1 GB |
| 网络 | 本地回环 (127.0.0.1) | — |

### Python 依赖

```
PyQt5 >= 5.15          # 图形界面框架
networkx >= 3.1        # 图数据结构
matplotlib >= 3.7      # 图表绘制
numpy >= 1.24          # 数值计算
```

---

## 3. 安装与启动

### 3.1 获取项目

```bash
git clone https://github.com/nurzat0300/lsa.git
cd lsa
```

> 如果无法访问 GitHub，可从师兄师姐处直接拷贝整个 `lsa` 文件夹。

### 3.2 安装依赖

**方式一：一键自动安装（推荐，Windows）**

双击 `ui启动器.bat`，脚本会自动完成：创建虚拟环境 → 安装依赖 → 启动程序。

**方式二：手动安装**

```bash
# 1. 创建虚拟环境
python -m venv .venv

# 2. 激活虚拟环境
#    Windows:
.venv\Scripts\activate
#    Linux / macOS:
source .venv/bin/activate

# 3. 安装依赖（国内用户建议使用清华镜像）
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 3.3 启动系统

| 方式 | 操作 |
|---|---|
| **图形界面** | `python run.py --ui` 或双击 `启动.bat` |
| **命令行模式** | `python run.py --config config/network_topology.json` |
| **诊断模式** | 双击 `诊断.bat`（检查环境 → 自动启动） |

### 3.4 验证安装

```bash
python -c "import PyQt5; import networkx; import numpy; import matplotlib; print('环境就绪')"
```

输出 "环境就绪" 表示安装成功。

---

## 4. 界面操作指南

> **本章是系统使用的核心内容，建议对照界面逐项操作一遍。**

程序主界面包含 **8 个标签页**，启动后默认显示"教学演示"页面。

### 4.1 📖 教学演示（默认页面）

**用途：** 为初学者设计的逐步学习指南，按顺序讲解链路状态路由协议的工作原理。

**8 个教学步骤：**

| 步骤 | 主题 | 核心内容 |
|---|---|---|
| 1 | 理解网络拓扑 | 什么是链路状态路由？拓扑数据库(LSDB)概念 |
| 2 | 邻居发现 | Hello 协议、心跳机制、邻居状态管理 |
| 3 | LSA 生成 | 链路状态通告的结构和生成过程 |
| 4 | LSA 洪泛 | 可靠洪泛机制、序列号去重、TTL 控制 |
| 5 | SPF 计算 | Dijkstra 算法核心思想：贪心 + 松弛 |
| 6 | 路由表生成 | 如何从最短路径树提取下一跳 |
| 7 | 链路故障 | 故障检测、重新收敛、备用路径 |
| 8 | 算法性能对比 | 不同算法的时间复杂度对比 |

**操作方式：**
- 左侧导航栏点击步骤名称切换
- 底部「上一步 / 下一步」按钮顺序浏览
- 建议按顺序学习，配合其他标签页动手验证

---

### 4.2 🔍 网络拓扑

**用途：** 实时可视化展示当前网络拓扑结构。

**操作说明：**

| 控件 | 功能 |
|---|---|
| **视角下拉框** | 选择观察视角——「全局视图」或以某个路由器视角查看 |
| **自动刷新** | 勾选后每 2 秒自动更新拓扑图 |
| **刷新拓扑** | 手动刷新一次 |
| **重置布局** | 重新计算节点位置（拖拽乱了之后使用） |

**节点颜色含义：**

| 颜色 | 含义 |
|---|---|
| 🔴 红色 | 当前视角路由器（以它为中心观察） |
| 🟢 绿色 | 已被视角路由器发现（已同步） |
| ⚪ 灰色 | 尚未被视角路由器发现（未同步） |
| 🔵 蓝色 | 全局视图下的普通节点 |

**教学提示：**
- 启动后切换到某个路由器视角，观察节点从灰色逐渐变绿——这就是 LSA 同步的过程
- 边的数字表示链路成本（cost），越小越优先
- 配合"仿真控制"标签页做链路故障实验，观察拓扑变化

---

### 4.3 🎬 算法动画

**用途：** 逐步演示 Dijkstra 算法执行过程，**适合课堂教学投屏展示**。

**操作步骤：**

| 步骤 | 操作 |
|---|---|
| 1 | 选择**源路由器**（算法起点） |
| 2 | （可选）选择**目标路由器**，用于高亮最短路径 |
| 3 | 点击 **▶ 播放** 自动播放，或手动点击 ◀ ▶ 逐步调试 |
| 4 | 拖动**速度滑块**调节播放速度（1 = 非常慢，10 = 极速） |
| 5 | 点击**高亮最短路径**查看起点到目标的最优路径 |
| 6 | 点击**跳转到步骤**直接跳到特定状态 |

**节点颜色含义：**

| 颜色 | 状态 |
|---|---|
| 🔴 红色 | 源节点 |
| 🟠 橙色 | 当前正在处理的节点 |
| 🟢 绿色 | 已永久标记（最短路径已确定） |
| 🔵 蓝色 | 边界节点（候选集，距离暂定） |
| ⚪ 灰色 | 未访问 |
| 红色虚线 | 正在松弛的边 |
| 绿色粗线 | 最终最短路径 |

**右侧面板：** 实时显示步骤文字描述 + 节点距离表。

**课堂教学建议：**
- 使用手动步进模式，每步暂停讲解"松弛"操作
- 切换不同源路由器，观察最短路径树的变化
- 结合距离表中 `∞ → 具体数值` 的变化讲解算法原理

---

### 4.4 📋 路由表

**用途：** 查看每个路由器的路由表详情。

**显示内容：**

| 列 | 说明 |
|---|---|
| 目标 | 目的节点 ID |
| 下一跳 | 数据包下一跳发给谁 |
| 成本 | 到达目标的总链路成本 |
| 完整路径 | 从源到目标的完整路径（如 1→3→5） |
| 跳数 | 经过的路由器数量 |

**下方信息面板：**
- 邻接路由器列表（直连邻居）
- 已知拓扑节点数（LSDB 中已知的路由器数量）
- LSA 序列号和已缓存 LSA 数目

> 数据每 2 秒自动刷新。切换路由器下拉框可查看不同路由器的视角。

---

### 4.5 📊 算法对比

**用途：** 运行 **8 种**最短路径算法的性能对比测试。

**操作步骤：**
1. 选择**源路由器**
2. 设置**测试轮数**（建议 10 ~ 50，轮数越多结果越稳定）
3. 点击**执行对比计算**（计算在后台线程运行，不阻塞界面）

**8 种对比算法：**

| 算法 | 类型 | 时间复杂度 | 说明 |
|---|---|---|---|
| **Dijkstra** | 贪心 + 优先队列 | O(E log V) | 经典最短路径算法 |
| **新型算法(分层桶)** | 桶优化 | O(E log V) | 分层桶减少比较次数 |
| **Duan2025** | 递归分治 | O(m log²/³ n) | STOC 2025 最佳论文 |
| **Bellman-Ford** | 动态规划 | O(VE) | 支持负权边 |
| **SPFA** | 队列优化 | O(kE) | Bellman-Ford 的工程优化 |
| **Prim(MST)** | 最小生成树 | O(E log V) | 非最短路径，作为对比基线 |
| **Floyd-Warshall** | 全源 DP | O(V³) | 计算所有节点对的最短路径 |
| **A\*** | 启发式搜索 | O(E) | 使用跳数启发式加速 |

**结果指标：** 平均 / 最小 / 最大耗时、平均访问节点数、平均迭代次数、平均松弛次数。

**教学实验建议：**
- 分别在 8 节点、20 节点、50 节点拓扑下运行对比
- 观察 Floyd-Warshall 在 50 节点下 O(V³) 的劣势
- 对比 Dijkstra 与 Duan2025 在大规模下的表现差异

---

### 4.6 ⚙️ 仿真控制

**用途：** 控制仿真运行，模拟网络事件。

| 功能区 | 操作 | 效果 |
|---|---|---|
| **拓扑切换** | 下拉选择拓扑 → 点击「加载并演示」 | 一键切换 8 / 20 / 50 节点 |
| **链路故障** | 输入两个路由器 ID → 点击「创建故障」 | 链路断开，全网重新收敛 |
| **链路恢复** | 输入两个路由器 ID + 成本 → 点击「恢复链路」 | 链路恢复，路由重新计算 |
| **新增路由器** | 输入 ID + 端口 → 点击「添加路由器」 | 动态加入新路由器 |
| **新增链路** | 输入两个路由器 ID + 成本 → 点击「添加链路」 | 动态加入新链路 |

**下方统计面板：**
- 各路由器状态（邻接数、活跃邻接、已知节点、路由条目）
- 40 节点以上自动切换汇总模式，避免卡顿

---

### 4.7 📡 LSA 洪泛（高级功能）

**用途：** 实时追踪 LSA 从产生到洪泛的全过程，可视化 LSA 的传播路径。

**界面组成：**

**上半部分 — 事件表格：**

| 列 | 说明 |
|---|---|
| 时间 | 事件发生的精确时间（含毫秒） |
| 事件 | 事件类型（生成 / 接受 / 丢弃 / 转发 / TTL耗尽） |
| LSA源 | 该 LSA 最初由哪个路由器产生 |
| 序列号 | LSA 的序列号（单调递增） |
| 详情 | 人类可读的事件描述 |

**事件颜色含义：**

| 颜色 | 事件类型 | 含义 |
|---|---|---|
| 🟢 绿色 | 生成 (CREATED) | 路由器主动生成新 LSA |
| 🔵 蓝色 | 接受新 (RECEIVED_NEW) | 收到更新的 LSA 并接受 |
| ⚪ 灰色 | 丢弃旧 (RECEIVED_OLD) | 收到的 LSA 比已有版本旧，丢弃 |
| 🟠 橙色 | 丢弃重复 (RECEIVED_DUP) | 收到相同序列号的 LSA，丢弃 |
| 🟣 紫色 | 洪泛转发 (FORWARDED) | 将 LSA 转发给所有邻居 |
| 🔴 红色 | TTL耗尽 (DISCARDED_TTL) | 跳数减到 0，停止转发 |

**下半部分 — 洪泛动画：**

- 拓扑图中节点颜色实时反映 LSA 的到达状态
- 紫色虚线表示 LSA 洪泛传播的路径
- 点击表格任意行可跳转到对应时刻的拓扑快照
- **「▶ 自动播放」**按钮按时间顺序逐帧回放全部传播过程
- 速度滑块调节播放速度

**教学用途：**
- 观察一条 LSA 如何在网络中逐跳扩散
- 理解序列号如何用于去重（同一 LSA 不会被重复处理）
- 观察 TTL 机制如何防止无限循环

---

### 4.8 💓 邻居状态（高级功能）

**用途：** 可视化显示邻居状态机和 Hello / Dead 定时器的实时状态。

**界面组成：**

- **路由器选择下拉框** — 选择以哪个路由器的视角观察邻居
- **邻居卡片** — 每个邻居一张卡片，包含：

| 信息 | 说明 |
|---|---|
| 邻居名称 | Router ID + 路由器名称 |
| 当前状态 | Down / Init / 2-Way / Full |
| 链路成本 | 到该邻居的链路开销 |
| 状态机进度条 | Down → Init → 2-Way → Full 四阶段可视化 |
| Hello 倒计时条 | 蓝色→绿色渐变，显示距上次 Hello 的时间 |
| Dead 超时条 | 黄色→红色渐变，显示超时计数进度 |

**邻居状态机：**

```
Down ──(收到Hello)──→ Init ──(双向通信)──→ 2-Way ──(LSA同步)──→ Full
  ↑                                                                   │
  └────────────────────(超时/链路故障)─────────────────────────────────┘
```

**颜色含义：**

| 颜色 | 状态 | 含义 |
|---|---|---|
| 🔴 红色 | Down | 邻居不可达 |
| 🟠 橙色 | Init | 收到第一个 Hello，正在初始化 |
| 🔵 蓝色 | 2-Way | 双向通信已确认 |
| 🟢 绿色 | Full | LSA 已同步，路由计算完成 |

**教学用途：**
- 理解 OSPF 邻居状态机的四个阶段
- 观察 Hello 定时器和 Dead 超时的工作方式
- 模拟链路故障时观察状态如何从 Full 回退到 Down

---

## 5. 实验操作示例

### 实验一：观察 LSA 洪泛与路由收敛

**目的：** 理解链路状态路由协议启动后拓扑逐步同步的过程。

**步骤：**

1. 启动系统，观察顶部状态栏显示 "已启动路由器: 12/12"
2. 切换到 **📡 LSA洪泛** 标签页
3. 观察事件表格实时滚动——可以看到 CREATED → FORWARDED → RECEIVED_NEW 事件序列
4. 点击「▶ 自动播放」，观察拓扑图中 LSA 如何从源头逐跳扩散
5. 切换到 **📋 路由表** 标签页，选择 Router1
6. 观察路由表条目从 0 逐渐增长到 11（约 5 ~ 10 秒）
7. 切换到 **🔍 网络拓扑** 标签页，选择 Router1 视角
8. 观察节点颜色从灰色逐渐变为绿色

**思考题：**
- 为什么启动后需要几秒路由表才能完整建立？
- LSA 洪泛过程中，"丢弃重复"事件为什么占大多数？

---

### 实验二：链路故障与重新收敛

**目的：** 观察链路故障后路由协议的自动恢复能力。

**步骤：**

1. **📋 路由表** → 选择 Router1 → 记录到 Router5 的路径和成本
2. **⚙️ 仿真控制** → 设置路由器1 = 1，路由器2 = 3 → 点击「创建故障」
3. 切回 **📋 路由表** → 观察 Router1 到 Router5 的路径是否自动切换（绕道）
4. **💓 邻居状态** → 选择 Router1 → 观察 Router3 的卡片状态从 Full 变为 Down
5. 回到 **⚙️ 仿真控制** → 恢复链路 1↔3（成本设为 8）
6. 观察路径是否恢复，状态机是否回到 Full

**思考题：**
- 故障后路径发生了什么变化？成本为什么可能变大？
- 邻居状态机的转换过程用了多长时间？

---

### 实验三：算法动画课堂演示

**目的：** 直观展示 Dijkstra 算法的工作过程。

**步骤：**

1. 切换到 **🎬 算法动画** 标签页
2. 源路由器选 Router1，速度调为「慢速」
3. 点击 **▶ 播放**，观察以下变化：
   - 节点颜色：灰色 → 蓝色（候选）→ 绿色（已访问）
   - 红色虚线 = 正在松弛的边
   - 右侧距离表数值逐步更新
4. 运行完毕后，选择目标路由器 → 点击「高亮最短路径」
5. 绿色粗线显示实际最短路径

**课堂技巧：** 手动步进模式 + 每步暂停讲解，效果最佳。

---

### 实验四：不同规模下的算法性能对比

**目的：** 理解时间复杂度在实际运行中的影响。

**步骤：**

1. **⚙️ 仿真控制** → 选择「小型拓扑（8节点）」→ 点击「加载并演示」
2. **📊 算法对比** → 选择 Router1，轮数 = 30 → 执行对比 → 记录各算法耗时
3. 依次测试「中型拓扑（20节点）」和「大型拓扑（50节点）」
4. 对比三种规模下各算法的耗时增长趋势

**预期观察：**

| 算法 | 8 节点 | 20 节点 | 50 节点 | 增长趋势 |
|---|---|---|---|---|
| Dijkstra | ~0.01ms | ~0.02ms | ~0.05ms | 平缓 |
| Floyd-Warshall | ~0.02ms | ~0.3ms | ~5ms+ | **急剧** |
| Duan2025 | ~0.02ms | ~0.04ms | ~0.08ms | 平缓 |

---

### 实验五：自定义网络拓扑

**目的：** 学习配置文件格式，创建自己的拓扑。

**步骤：**

1. 复制 `config/test_topology1.json` → `config/my_topology.json`
2. 用文本编辑器编辑，自定义路由器和链路（格式见第 7 章）
3. 命令行启动：`python run.py --config config/my_topology.json --ui`

**要求：** 拓扑必须是连通图（任意两点间有路径可达）。

---

## 6. 如何添加新算法

> 系统设计了一套标准的算法接口，添加新算法只需修改 **3 个文件**。
> 下面以添加"双向 Dijkstra (Bidirectional Dijkstra)"为例，给出完整流程。

### 第一步：编写算法类

在 `src/algorithms/` 目录下新建 Python 文件，例如 `bidirectional_dijkstra.py`。

你的算法类**必须**实现两个方法：`calculate_shortest_paths()` 和 `get_statistics()`。

```python
"""
双向Dijkstra算法 — 从起点和终点同时搜索
"""
import heapq
import time
from typing import Dict, List, Tuple


class BidirectionalDijkstra:
    """双向Dijkstra最短路径算法"""

    def __init__(self):
        self.computation_time = 0.0
        self.visited_nodes = 0
        self.iterations = 0

    def calculate_shortest_paths(
        self,
        source: int,
        graph: Dict[int, List[Tuple[int, int]]],
    ) -> Dict[int, Tuple[int, List[int]]]:
        """
        计算从 source 到所有其他节点的最短路径。

        参数:
            source: 源节点 ID（整数）
            graph:  邻接表 {节点: [(邻居, 边权), ...]}

        返回:
            {目标节点: (总成本, [完整路径]), ...}
            不包含 source → source 自身
        """
        start_time = time.perf_counter()
        self.visited_nodes = 0
        self.iterations = 0

        # ==== 在此实现你的算法 ====
        distances = {source: 0}
        paths = {source: [source]}
        visited = set()
        pq = [(0, source)]

        while pq:
            self.iterations += 1
            curr_cost, curr_node = heapq.heappop(pq)
            if curr_node in visited:
                continue
            visited.add(curr_node)
            self.visited_nodes += 1

            if curr_node in graph:
                for neighbor, cost in graph[curr_node]:
                    new_cost = curr_cost + cost
                    if neighbor not in distances or new_cost < distances[neighbor]:
                        distances[neighbor] = new_cost
                        paths[neighbor] = paths[curr_node] + [neighbor]
                        heapq.heappush(pq, (new_cost, neighbor))

        self.computation_time = time.perf_counter() - start_time

        result = {}
        for dest in distances:
            if dest != source:
                result[dest] = (distances[dest], paths.get(dest, []))
        return result

    def get_statistics(self) -> Dict:
        """返回算法统计信息（必须实现，用于算法对比表格）"""
        return {
            'algorithm_name': 'BidirectionalDijkstra',
            'computation_time': self.computation_time * 1000,  # 毫秒
            'visited_nodes': self.visited_nodes,
            'iterations': self.iterations,
        }
```

### 第二步：注册算法

需要修改 **3 个位置**：

**位置 1 — `src/algorithms/__init__.py`**

```python
from .bidirectional_dijkstra import BidirectionalDijkstra   # ← 新增

__all__ = [
    'DijkstraAlgorithm',
    'NewAlgorithm',
    'DuanAlgorithm',
    'BellmanFordAlgorithm',
    'SPFAAlgorithm',
    'PrimMSTBaseline',
    'FloydWarshallAlgorithm',
    'AStarAllTargetsAlgorithm',
    'BidirectionalDijkstra',     # ← 新增
]
```

**位置 2 — `src/core/protocol.py`**

```python
# ① 文件顶部添加导入
from ..algorithms.bidirectional_dijkstra import BidirectionalDijkstra

class PathCalculator:
    def __init__(self, ...):
        # ② 实例化
        self.bidir_algo = BidirectionalDijkstra()

        # ③ 注册到 algorithm_registry
        self.algorithm_registry = {
            # ... 已有的 ...
            'bidirectional': ('BidirectionalDijkstra', self.bidir_algo),  # ← 新增
        }
```

**位置 3 — `src/ui/main_window.py`**

在 `AlgorithmComparisonWidget` 类中：

```python
# ① __init__ 中添加显示名
self.algorithm_display_map = {
    # ... 已有的 ...
    'bidirectional': '双向Dijkstra',    # ← 新增
}

# ② _on_benchmark_finished 中添加排序
ordered_keys = [
    'dijkstra', 'new_algo', 'duan', 'bellman_ford',
    'spfa', 'prim_mst', 'floyd_warshall', 'astar',
    'bidirectional',    # ← 新增
]
```

### 第三步：验证

1. 启动系统：`python run.py --ui`
2. 切换到 **📊 算法对比** 标签页
3. 选择任意路由器，点击「执行对比计算」
4. 确认"双向Dijkstra"出现在表格中，且各项数据合理

### 算法接口规范（参考）

```python
def calculate_shortest_paths(
    self,
    source: int,
    graph: Dict[int, List[Tuple[int, int]]],
) -> Dict[int, Tuple[int, List[int]]]:
    """
    参数:
        source: 源节点 ID（整数）
        graph:  邻接表，{节点: [(邻居, 边权), ...]}

    返回:
        {目标节点: (总成本, [完整路径]), ...}
        不包含 source → source 自身
    """

def get_statistics(self) -> Dict:
    """
    返回:
        {
            'algorithm_name':    str,     # 算法名称
            'computation_time':  float,   # 耗时（毫秒）
            'visited_nodes':     int,     # 访问节点数
            'iterations':        int,     # 迭代次数
            # 可选字段：
            'relaxation_count':  int,     # 松弛操作次数
        }
    """
```

### 添加算法检查清单

- [ ] 新建 `src/algorithms/xxx.py`，实现 `calculate_shortest_paths()` 和 `get_statistics()`
- [ ] 在 `src/algorithms/__init__.py` 中导入并导出
- [ ] 在 `src/core/protocol.py` 中实例化并注册
- [ ] 在 `src/ui/main_window.py` 中添加显示名称和排序
- [ ] 启动系统，在算法对比标签页中验证

> **提示：** 只要遵循接口规范，你的算法就会自动出现在算法对比表格中，无需修改 UI 的其他部分。

---

## 7. 配置文件说明

拓扑配置文件为 JSON 格式，位于 `config/` 目录。

### 7.1 配置文件结构

```json
{
  "routers": [
    {
      "router_id": 1,
      "router_name": "Router1",
      "listen_port": 21001,
      "listen_ip": "127.0.0.1"
    }
  ],
  "links": [
    {
      "router1_id": 1,
      "router2_id": 2,
      "cost": 4
    }
  ]
}
```

### 7.2 字段说明

**routers 数组：**

| 字段 | 类型 | 说明 |
|---|---|---|
| router_id | int | 路由器唯一标识（1 ~ 255） |
| router_name | string | 名称，用于界面显示 |
| listen_port | int | UDP 端口（10000 ~ 65535），不可重复 |
| listen_ip | string | 本地用 127.0.0.1，分布式部署改为实际 IP |

**links 数组：**

| 字段 | 类型 | 说明 |
|---|---|---|
| router1_id | int | 链路一端路由器 ID |
| router2_id | int | 链路另一端路由器 ID |
| cost | int | 链路成本（1 ~ 65535），越小越优先 |

### 7.3 现有拓扑文件

| 文件 | 节点 | 链路 | 用途 |
|---|---|---|---|
| `topology_small_demo.json` | 8 | 13 | 快速演示 / 课堂教学 |
| `network_topology.json` | 12 | 20 | **默认拓扑** |
| `topology_medium_demo.json` | 20 | 35 | 中型测试 |
| `benchmark_topology_50.json` | 50 | 90 | 压力测试 / 算法性能对比 |
| `test_topology1.json` | 5 | 7 | 单元测试 |
| `test_topology2.json` | 5 | 7 | 故障恢复测试 |
| `test_topology3.json` | 4 | 5 | 路由表验证 |

### 7.4 注意事项

1. **链路成本必须 ≥ 1**
2. 拓扑必须是**连通图**（任意两点间有路径可达）
3. 端口不能冲突，建议从 21001 递增
4. 50 节点及以上自动切换**稳态参数**（降低心跳频率、延长超时阈值、降级日志）

---

## 8. 常见问题

### Q1：启动报错 "DLL load failed" 或 "找不到 Qt 平台插件"

**原因：** PyQt5 安装不完整或版本不兼容。

**解决：**
```bash
pip uninstall PyQt5 PyQt5-sip -y
pip install PyQt5==5.15.9 PyQt5-sip==12.13.0
```

### Q2：防火墙提示网络访问

**原因：** 路由器之间使用本地 UDP（127.0.0.1）通信。

**解决：** 点击「允许访问」，或在防火墙中添加 Python 例外。本系统仅在本地通信，不会连接外部网络。

### Q3：端口被占用

**原因：** 上次程序未正常退出，Python 进程仍占用端口。

**解决：**
1. 任务管理器结束所有 `python.exe` 进程
2. 或在配置文件中更换端口号

### Q4：50 节点拓扑界面卡顿

**原因：** 大量路由表渲染开销。

**解决：** 系统已自动切换汇总模式。如仍卡顿：
- 减少同时打开的标签页数量
- 编辑 `src/ui/main_window.py`，将 `timer.start(2000)` 的数值调大（如 5000）

### Q5：如何修改心跳检测间隔？

编辑 `src/core/constants.py`：
```python
HEARTBEAT_INTERVAL = 0.8   # 心跳发送间隔（秒）
HEARTBEAT_TIMEOUT = 3      # 连续超时次数阈值
```

### Q6：如何在多台机器上分布式运行？

1. 修改配置文件中的 `listen_ip` 为各机器实际 IP 地址
2. 确保各机器网络互通，防火墙放行对应 UDP 端口
3. 将相同的配置文件复制到所有机器
4. 各机器分别启动 `python run.py --config <配置路径> --ui`

### Q7：GitHub 下载太慢或连不上？

- 使用 VPN 或代理
- 使用 GitHub 镜像站（如 kgithub.com）
- 从师兄师姐处直接拷贝项目文件夹

### Q8：LSA 洪泛标签页事件数量太多？

这是正常现象——每次 LSA 交换都会产生多条事件（生成、转发、重复检测等）。12 节点默认拓扑稳定后约产生数百条事件。可以通过「过滤源」下拉框缩小范围，或点击「清空日志」重置。

---

## 9. 项目结构

```
lsa/
├── run.py                      # 主入口（python run.py --ui）
├── requirements.txt            # Python 依赖
├── .gitignore
├── 使用手册.md                  # 本手册
├── 实验手册.md                  # 旧版实验手册
├── ui启动器.bat                 # 一键安装 + 启动（推荐首次使用）
├── 简易启动.bat                 # 快速启动
├── 启动.bat                     # 标准启动
├── 诊断.bat                     # 诊断模式（检查环境 → 启动）
│
├── config/                      # 网络拓扑配置文件（JSON）
│   ├── network_topology.json    # 默认 12 节点拓扑
│   ├── topology_small_demo.json # 8 节点快速演示
│   ├── topology_medium_demo.json# 20 节点中型测试
│   ├── benchmark_topology_50.json# 50 节点压力测试
│   └── test_topology*.json      # 单元测试拓扑
│
├── src/
│   ├── simulator.py             # 仿真主控制器
│   │
│   ├── algorithms/              # ★ 添加新算法在这里
│   │   ├── dijkstra.py          # Dijkstra 算法
│   │   ├── new_algorithm.py     # 分层桶优化算法
│   │   ├── duan_algorithm.py    # Duan2025 (STOC 2025)
│   │   ├── extended_algorithms.py # Bellman-Ford / SPFA / Prim / Floyd / A*
│   │   ├── stepped_dijkstra.py  # 分步执行 Dijkstra（算法动画用）
│   │   └── __init__.py          # ★ 新算法需在此注册
│   │
│   ├── core/                    # 协议与数据结构
│   │   ├── constants.py         # 常量定义
│   │   ├── lsa.py               # LSA 数据包 + 全局事件日志
│   │   ├── protocol.py          # ★ 新算法需在此注册
│   │   ├── router.py            # 路由器核心（UDP通信 / 线程管理）
│   │   └── topology.py          # 拓扑数据库 + 路由表
│   │
│   └── ui/                      # PyQt5 图形界面
│       ├── main_window.py       # ★ 主窗口 + 新算法显示名
│       ├── styles.py            # 全局样式表
│       ├── teaching_demo.py     # 教学演示（8步教程）
│       ├── topology_view.py     # 网络拓扑可视化
│       ├── animation_view.py    # Dijkstra 算法动画
│       ├── lsa_monitor.py       # LSA 洪泛事件追踪 + 动画
│       └── neighbor_monitor.py  # 邻居状态机 + Hello/Dead 定时器
│
└── tests/
    └── run_tests.py             # 自动化测试
```

### 系统架构

```
┌─────────────────────────────────────────────────────┐
│                UI 层 (PyQt5) — 8 个标签页             │
│  教学演示 │ 拓扑显示 │ 算法动画 │ 路由表              │
│  算法对比 │ 仿真控制 │ LSA洪泛 │ 邻居状态             │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│           仿真系统层 (RoutingSimulator)               │
│  - 路由器实例管理  - 拓扑切换  - 故障注入             │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│              路由器层 (Router × N)                   │
│  UDP通信 │ LSA洪泛 │ 心跳检测 │ 6个工作线程           │
└──────────────────────┬──────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────┐
│              协议与算法层                             │
│  PathCalculator ← 8种最短路径算法                    │
│  TopologyDB + RoutingTable + LSADatabase             │
│  LSAEventLog (全局事件追踪)                          │
└─────────────────────────────────────────────────────┘
```

---

## 参考资料

- Dijkstra, E. W. (1959). *A note on two problems in connexion with graphs.*
- RFC 2328 — OSPF Version 2
- Duan, Mao, Mao, Shu, Yin (2025). *Breaking the Sorting Barrier for Directed SSSP.* STOC 2025.
- Kurose & Ross. *Computer Networking: A Top-Down Approach* (8th Edition)
- NetworkX 文档: https://networkx.org/
- PyQt5 文档: https://www.riverbankcomputing.com/static/Docs/PyQt5/

---

*本手册面向计算机网络课程的教师和学生，以及后续维护该项目的开发者。*

*项目地址：https://github.com/nurzat0300/lsa*
