# 链路状态路由协议分布式仿真系统 — 实验手册

---

## 目录

1. [系统简介](#1-系统简介)
2. [环境要求](#2-环境要求)
3. [从 GitHub 下载与安装](#3-从-github-下载与安装)
4. [快速启动](#4-快速启动)
5. [界面操作指南](#5-界面操作指南)
6. [实验操作示例](#6-实验操作示例)
7. [如何添加新算法](#7-如何添加新算法)
8. [配置文件说明](#8-配置文件说明)
9. [命令行模式](#9-命令行模式)
10. [常见问题](#10-常见问题)
11. [项目结构](#11-项目结构)

---

## 1. 系统简介

本系统是一个**链路状态路由协议（Link State Routing Protocol）的分布式仿真平台**，用于计算机网络课程教学。系统模拟了 OSPF 协议的核心机制，包括邻居发现、LSA 洪泛、SPF 最短路径计算和路由表生成。

### 核心功能

| 功能 | 说明 |
|---|---|
| 分布式路由器仿真 | 每个路由器独立运行，通过 UDP 进行通信 |
| Hello 协议 | 心跳检测与邻居发现 |
| LSA 洪泛 | 可靠的链路状态通告传播机制 |
| SPF 计算 | 基于 Dijkstra 算法的最短路径优先计算 |
| 多算法对比 | 集成 7 种最短路径算法的性能对比 |
| 算法动画 | Dijkstra 算法的分步执行可视化 |
| 故障模拟 | 链路故障与恢复的动态演示 |
| 拓扑切换 | 支持 8/12/20/50 节点拓扑一键切换 |

### 适用课程

- 计算机网络（路由协议章节）
- 数据结构与算法（图论最短路径）
- 分布式系统（一致性、洪泛机制）

---

## 2. 环境要求

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows 10/11、Linux、macOS |
| Python | 3.8 ~ 3.11（推荐 3.10） |
| Git | 用于从 GitHub 下载项目 |
| 内存 | ≥ 2 GB |
| 磁盘 | ≥ 500 MB（含虚拟环境） |

### Python 依赖

```
PyQt5 >= 5.15        # 图形界面框架
PyQt5-sip >= 12.13   # PyQt5 辅助库
numpy >= 1.24         # 数值计算
networkx >= 3.1       # 图数据结构
matplotlib >= 3.7     # 图表绘制
```

---

## 3. 从 GitHub 下载与安装

### 3.1 下载项目

项目托管在 GitHub，使用 `git clone` 下载：

```bash
# 打开终端（Windows 推荐 Git Bash 或 PowerShell），进入你想存放的目录
cd D:\

# 克隆项目
git clone https://github.com/nurzat0300/lsa.git

# 进入项目目录
cd lsa
```

> 如果无法访问 GitHub，可以使用代理或从师兄师姐处拷贝项目文件夹。

### 3.2 安装依赖

**方式一：一键自动安装（推荐，仅 Windows）**

双击项目目录中的 `ui启动器.bat`，脚本会**自动创建虚拟环境 → 安装所有依赖 → 启动界面**，全程无需手动干预。

**方式二：手动安装（适用 Windows / Linux / macOS）**

```bash
# 1. 创建虚拟环境
python -m venv .venv

# 2. 激活虚拟环境
#    Windows:
.venv\Scripts\activate
#    Linux / macOS:
source .venv/bin/activate

# 3. 安装依赖（国内用户建议使用清华镜像加速）
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

**方式三：仅安装依赖（已有 Python 环境）**

```bash
pip install -r requirements.txt
```

### 3.3 验证安装

```bash
python -c "import PyQt5; import networkx; import numpy; import matplotlib; print('依赖安装成功')"
```

如果输出了"依赖安装成功"，说明环境已就绪。

---

## 4. 快速启动

### 图形界面模式

```bash
python run.py
```

或双击以下任一 bat 文件（Windows）：

| 文件 | 用途 |
|---|---|
| `ui启动器.bat` | **推荐** — 自动创建虚拟环境、安装依赖、启动 UI |
| `简易启动.bat` | 跳过依赖检查，直接启动 |
| `启动.bat` | 标准启动 |
| `诊断.bat` | 诊断模式，输出调试信息 |

### 启动后效果

程序启动后将自动加载默认拓扑（12 节点），各路由器开始互相发送 Hello 消息和 LSA，拓扑逐步同步。界面显示 6 个标签页，默认显示"教学演示"页面。顶部状态栏显示当前路由器数量和活跃链路数。

---

## 5. 界面操作指南

> **本章是系统使用的核心内容，建议对照界面逐项操作一遍。**

程序主界面包含 **6 个标签页**。

### 5.1 📖 教学演示（默认页面）

**用途：** 为初学者设计的逐步学习指南，按顺序讲解链路状态路由协议的工作原理。

**操作：**
- 左侧导航栏点击步骤（共 8 步），右侧显示对应知识
- 底部"上一步 / 下一步"按钮切换页面
- 建议按顺序学习

**8 个教学步骤：**
1. 理解网络拓扑
2. 邻居发现（Hello 协议）
3. LSA 生成
4. LSA 洪泛（Flooding）
5. SPF 计算（Dijkstra 算法）
6. 路由表生成
7. 链路故障与收敛
8. 算法性能对比

### 5.2 🔍 网络拓扑

**用途：** 实时可视化展示当前网络拓扑结构。

**操作：**
- 下拉菜单选择**视角路由器**（以不同路由器视角查看拓扑）
- 鼠标可拖拽节点、滚轮缩放
- 绿色节点 = 已同步，灰色节点 = 未同步
- 边上数字 = 链路成本

### 5.3 🎬 算法动画

**用途：** 逐步演示 Dijkstra 算法执行过程，**适合课堂教学投屏展示**。

**操作步骤：**

| 步骤 | 操作 |
|---|---|
| 1 | 选择**源路由器**（算法起点） |
| 2 | （可选）选择**目标路由器**用于高亮最短路径 |
| 3 | 点击 **▶ 播放** 开始自动播放，或手动点击 ◀ ▶ 逐步调试 |
| 4 | 拖动**速度滑块**调节播放速度（1=非常慢, 10=极速） |
| 5 | 点击**跳转到步骤**直接查看特定状态 |
| 6 | 点击**高亮最短路径**查看最终结果 |

**图例说明：**
- 🔴 红色节点 = 源节点
- 🟠 橙色节点 = 当前处理节点
- 🟢 绿色节点 = 已永久标记
- 🔵 蓝色节点 = 边界节点（候选集）
- ⚪ 灰色节点 = 未访问
- 红色虚线 = 松弛操作中的边
- 绿色粗线 = 最终最短路径

**右侧面板：** 实时显示步骤文字描述 + 节点距离表。

**课堂教学建议：**
- 使用手动步进模式，每步暂停讲解"松弛"操作
- 切换不同源路由器，观察最短路径树的变化
- 结合距离表中 ∞ → 具体数值的变化讲解算法原理

### 5.4 📋 路由表

**用途：** 查看每个路由器的路由表详情。

**操作：**
- 下拉菜单选择路由器
- 表格显示：目标节点、下一跳、总成本、完整路径、跳数
- 下方显示邻居列表、已知拓扑节点数、LSA 序列号
- 数据每 2 秒自动刷新

### 5.5 📊 算法对比

**用途：** 运行 7 种最短路径算法的性能对比测试。

**操作步骤：**
1. 选择**源路由器**
2. 设置**测试轮数**（建议 10-50，轮数越多结果越稳定）
3. 点击**执行对比计算**（计算在后台线程运行，不阻塞界面）

**对比算法一览：**

| 算法 | 类型 | 时间复杂度 |
|---|---|---|
| Dijkstra | 贪心 + 优先队列 | O(E log V) |
| 新型算法 | 分层桶优化 | O(E log V) |
| Bellman-Ford | 动态规划 | O(VE) |
| SPFA | 队列优化 Bellman-Ford | O(kE) |
| Prim (MST基线) | 最小生成树 | O(E log V) |
| Floyd-Warshall | 全源动态规划 | O(V³) |
| A* | 启发式搜索 | O(E) |

**结果指标：** 平均/最小/最大耗时、平均访问节点数、平均迭代次数、平均松弛次数。

### 5.6 ⚙️ 仿真控制

**用途：** 控制仿真运行，模拟网络事件。

| 功能 | 操作 | 效果 |
|---|---|---|
| **拓扑切换** | 下拉选择拓扑 → 点击"加载并演示" | 一键切换 8/12/20/50 节点 |
| **链路故障** | 输入两个路由器 ID → 点击"创建故障" | 链路断开，全网重新收敛 |
| **链路恢复** | 输入两个路由器 ID + 成本 → 点击"恢复链路" | 链路恢复，路由重新计算 |
| **新增路由器** | 输入 ID + 端口 → 点击"添加路由器" | 动态加入新路由器 |
| **新增链路** | 输入两个路由器 ID + 成本 → 点击"添加链路" | 动态加入新链路 |

下方统计面板显示各路由器状态；40 节点以上自动切换汇总模式避免卡顿。

---

## 6. 实验操作示例

### 实验一：观察 LSA 洪泛与路由收敛

**目的：** 理解链路状态路由协议启动后拓扑逐步同步的过程。

**步骤：**
1. 启动系统，观察顶部状态栏 "已启动路由器: 12/12"
2. 切换到**路由表**标签页，选择 Router1
3. 观察路由表条目从 0 逐渐增长到 11（约 5-10 秒）
4. 观察下方"已知拓扑节点"数量的同步增长
5. 切换到**网络拓扑**标签页，观察节点颜色从灰色 → 绿色

**思考题：** 为什么启动后需要几秒路由表才能完整建立？

### 实验二：链路故障与重新收敛

**目的：** 观察链路故障后路由协议的自动恢复能力。

**步骤：**
1. **路由表**标签页 → 选择 Router1 → 记录到 Router5 的路径和成本
2. **仿真控制**标签页 → 设置路由器1=1, 路由器2=3 → 点击**创建故障**
3. 切回**路由表** → 观察 Router1 到 Router5 的路径是否自动切换（绕道）
4. 回到**仿真控制** → 恢复链路 1↔3（成本设为 8）→ 观察路径是否恢复

**思考题：** 故障后路径发生了什么变化？成本为什么可能变大？

### 实验三：算法动画课堂演示

**目的：** 直观展示 Dijkstra 算法的工作过程。

**步骤：**
1. 切换到**算法动画**标签页
2. 源路由器选 Router1，速度调为"慢速"
3. 点击**▶ 播放**，观察：
   - 节点：灰色 → 蓝色（候选）→ 绿色（已访问）
   - 红色虚线 = 正在松弛的边
   - 右侧距离表数值逐步更新
4. 运行完毕后，选择目标路由器 → 点击**高亮最短路径**
5. 绿色粗线显示实际最短路径

**课堂技巧：** 手动步进模式 + 每步暂停讲解，效果最佳。

### 实验四：不同规模下的算法性能对比

**目的：** 理解时间复杂度在实际运行中的影响。

**步骤：**
1. **仿真控制** → 选择"小型拓扑（8节点）" → 加载并演示
2. **算法对比** → 选择 Router1，轮数=30 → 执行对比 → 记录耗时
3. 依次测试"中型拓扑（20节点）"和"大型拓扑（50节点）"
4. 对比三种规模下各算法的耗时增长趋势

**预期观察：**
- Floyd-Warshall 在 50 节点时 O(V³) 劣势极其明显
- Dijkstra 和新型算法耗时增长平缓
- A* 在大规模下访问节点数显著减少

### 实验五：自定义网络拓扑

**目的：** 学习配置文件格式，创建自己的拓扑。

**步骤：**
1. 复制 `config/test_topology1.json` → `config/my_topology.json`
2. 用文本编辑器编辑，自定义路由器和链路（格式见第 8 章）
3. 命令行启动：`python run.py --config config/my_topology.json`

---

## 7. 如何添加新算法

> **这是本手册最重要的章节之一。** 系统设计了一套标准的算法接口，添加新算法只需修改 **3 个文件**。
> 下面以添加"双向 Dijkstra (Bidirectional Dijkstra)"为例，给出完整流程。

### 第一步：编写算法类

在 `src/algorithms/` 目录下新建 Python 文件，比如 `bidirectional_dijkstra.py`。

你的算法类**必须**实现两个方法：`calculate_shortest_paths()` 和 `get_statistics()`。

```python
"""
双向Dijkstra算法 - 从起点和终点同时搜索
"""
import heapq
import time
from typing import Dict, List, Tuple


class BidirectionalDijkstra:
    """双向Dijkstra最短路径算法"""

    def __init__(self):
        self.computation_time = 0.0
        self.visited_nodes = 0
        self.iterations = 0

    def calculate_shortest_paths(
        self,
        source: int,
        graph: Dict[int, List[Tuple[int, int]]],
    ) -> Dict[int, Tuple[int, List[int]]]:
        """
        计算从 source 到所有其他节点的最短路径。

        Args:
            source: 源节点 ID
            graph: 邻接表 {node: [(neighbor, cost), ...]}

        Returns:
            {destination: (total_cost, [path_nodes])}
            注意：不包含 source → source 自身
        """
        start_time = time.perf_counter()
        self.visited_nodes = 0
        self.iterations = 0

        # ========== 你的算法实现 ==========
        distances = {source: 0}
        paths = {source: [source]}
        visited = set()
        pq = [(0, source)]

        while pq:
            self.iterations += 1
            curr_cost, curr_node = heapq.heappop(pq)
            if curr_node in visited:
                continue
            visited.add(curr_node)
            self.visited_nodes += 1

            if curr_node in graph:
                for neighbor, cost in graph[curr_node]:
                    new_cost = curr_cost + cost
                    if neighbor not in distances or new_cost < distances[neighbor]:
                        distances[neighbor] = new_cost
                        paths[neighbor] = paths[curr_node] + [neighbor]
                        heapq.heappush(pq, (new_cost, neighbor))

        self.computation_time = time.perf_counter() - start_time

        result = {}
        for dest in distances:
            if dest != source:
                result[dest] = (distances[dest], paths.get(dest, []))
        return result

    def get_statistics(self) -> Dict:
        """返回算法统计信息（必须实现，用于算法对比表格）"""
        return {
            'algorithm_name': 'BidirectionalDijkstra',
            'computation_time': self.computation_time * 1000,  # 毫秒
            'visited_nodes': self.visited_nodes,
            'iterations': self.iterations,
        }
```

### 第二步：注册算法

这一步需要修改 **3 个位置**，让系统知道新算法的存在。

**位置 1 — `src/algorithms/__init__.py`**

在文件末尾添加导入和导出：

```python
from .bidirectional_dijkstra import BidirectionalDijkstra   # ← 新增

__all__ = [
    'DijkstraAlgorithm',
    'NewAlgorithm',
    'DuanAlgorithm',
    'BellmanFordAlgorithm',
    'SPFAAlgorithm',
    'PrimMSTBaseline',
    'FloydWarshallAlgorithm',
    'AStarAllTargetsAlgorithm',
    'BidirectionalDijkstra',     # ← 新增
]
```

**位置 2 — `src/core/protocol.py`**

在 `PathCalculator` 类中完成三处修改：

```python
# ① 文件顶部，添加导入
from ..algorithms.bidirectional_dijkstra import BidirectionalDijkstra

class PathCalculator:
    def __init__(self, ...):
        # ... 其他代码 ...

        # ② 实例化你的算法
        self.bidir_algo = BidirectionalDijkstra()

        # ③ 注册到 algorithm_registry
        # key:    用于内部标识（字母+下划线）
        # value:  (显示名称, 算法实例)
        self.algorithm_registry = {
            'dijkstra':       ('Dijkstra',            self.dijkstra_algo),
            'new_algo':       ('NewAlgorithm',        self.new_algo),
            'bellman_ford':   ('BellmanFord',         self.bellman_ford_algo),
            'spfa':           ('SPFA',                self.spfa_algo),
            'prim_mst':       ('PrimMSTBaseline',     self.prim_mst_algo),
            'floyd_warshall': ('FloydWarshall',       self.floyd_warshall_algo),
            'astar':          ('AStarAllTargets',     self.astar_algo),
            'duan':           ('Duan2025',            self.duan_algo),
            'bidirectional':  ('BidirectionalDijkstra', self.bidir_algo),  # ← 新增
        }
```

**位置 3 — `src/ui/main_window.py`**

在 `AlgorithmComparisonWidget` 类中添加显示名称和排序：

```python
# ① __init__ 方法中，添加中文显示名
self.algorithm_display_map = {
    'dijkstra':       'Dijkstra算法',
    'new_algo':       '新型算法',
    'bellman_ford':   'Bellman-Ford算法',
    'spfa':           'SPFA算法',
    'prim_mst':       'Prim(MST基线)',
    'floyd_warshall': 'Floyd-Warshall算法',
    'astar':          'A*算法(多目标)',
    'bidirectional':  '双向Dijkstra',    # ← 新增
}

# ② _on_benchmark_finished 方法中，添加排序键
ordered_keys = [
    'dijkstra',
    'new_algo',
    'bellman_ford',
    'spfa',
    'prim_mst',
    'floyd_warshall',
    'astar',
    'bidirectional',    # ← 新增
]
```

### 第三步：验证

1. 启动系统：`python run.py`
2. 切换到**算法对比**标签页
3. 选择任意路由器，点击**执行对比计算**
4. 确认"双向Dijkstra"出现在表格中，且耗时、访问节点数等数据合理

### 算法接口规范（参考）

```python
def calculate_shortest_paths(
    self,
    source: int,
    graph: Dict[int, List[Tuple[int, int]]],
) -> Dict[int, Tuple[int, List[int]]]:
    """
    参数:
        source: 源节点 ID（整数）
        graph:  邻接表，{节点: [(邻居, 边权), ...]}

    返回:
        {目标节点: (总成本, [完整路径]), ...}
        不包含 source → source 自身
    """

def get_statistics(self) -> Dict:
    """
    返回:
        {
            'algorithm_name':    str,     # 算法名称（用于显示）
            'computation_time':  float,   # 耗时（毫秒）
            'visited_nodes':     int,     # 访问节点数
            'iterations':        int,     # 迭代次数
            # 可选字段：
            'relaxation_count':  int,     # 松弛操作次数
        }
    """
```

### 添加算法检查清单

- [ ] 新建 `src/algorithms/xxx.py`，实现 `calculate_shortest_paths()` 和 `get_statistics()`
- [ ] 在 `src/algorithms/__init__.py` 中导入并导出
- [ ] 在 `src/core/protocol.py` 中实例化并注册
- [ ] 在 `src/ui/main_window.py` 中添加显示名称和排序
- [ ] 启动系统，在算法对比标签页中验证

> **提示：** 只要遵循接口规范，你的算法就会自动出现在算法对比表格中，无需修改 UI 的其他部分。

---

## 8. 配置文件说明

拓扑配置文件为 JSON 格式，位于 `config/` 目录。

### 8.1 配置文件结构

```json
{
  "routers": [
    {
      "router_id": 1,
      "router_name": "Router1",
      "listen_port": 21001,
      "listen_ip": "127.0.0.1"
    }
  ],
  "links": [
    {
      "router1_id": 1,
      "router2_id": 2,
      "cost": 4
    }
  ]
}
```

### 8.2 字段说明

**routers 数组：**

| 字段 | 类型 | 说明 |
|---|---|---|
| router_id | int | 路由器唯一标识（1-255） |
| router_name | string | 名称，用于界面显示 |
| listen_port | int | UDP 端口（10000-65535），不可重复 |
| listen_ip | string | 本地用 127.0.0.1，分布式部署改为实际 IP |

**links 数组：**

| 字段 | 类型 | 说明 |
|---|---|---|
| router1_id | int | 链路一端路由器 ID |
| router2_id | int | 链路另一端路由器 ID |
| cost | int | 链路成本（1-65535），越小越优先 |

### 8.3 现有拓扑文件

| 文件 | 节点 | 链路 | 用途 |
|---|---|---|---|
| topology_small_demo.json | 8 | 13 | 快速演示 |
| network_topology.json | 12 | 20 | 默认拓扑 |
| topology_medium_demo.json | 20 | 35 | 中型测试 |
| benchmark_topology_50.json | 50 | 90 | 压力测试 |
| test_topology1.json | 5 | 7 | 单元测试 |
| test_topology2.json | 5 | 7 | 故障恢复测试 |
| test_topology3.json | 4 | 5 | 路由表验证 |

### 8.4 注意事项

1. 链路成本必须 ≥ 1
2. 拓扑必须是**连通图**（任意两点间有路径）
3. 端口不能冲突，建议从 21001 递增
4. 50 节点及以上自动切换**稳态参数**（降低心跳频率、延长超时阈值）

---

## 9. 命令行模式

系统也支持无 GUI 的命令行模式，适合远程运行和自动化测试。

### 启动

```bash
python run.py --config config/network_topology.json --cli
```

### 可用命令

| 命令 | 说明 | 示例 |
|---|---|---|
| `summary` | 打印所有路由表摘要 | `summary` |
| `topology` | 以 JSON 输出全网拓扑 | `topology` |
| `routes <id>` | 查看路由表 | `routes 1` |
| `fail <a> <b>` | 模拟链路故障 | `fail 1 2` |
| `recover <a> <b> <c>` | 恢复链路 | `recover 1 2 10` |
| `help` | 显示帮助 | `help` |
| `quit` | 退出 | `quit` |

### 运行测试

```bash
python tests/run_tests.py
```

---

## 10. 常见问题

### Q1：启动报错 "DLL load failed" 或 "找不到 Qt 平台插件"

**原因：** PyQt5 安装不完整或版本不兼容。

**解决：**
```bash
pip uninstall PyQt5 PyQt5-sip -y
pip install PyQt5==5.15.9 PyQt5-sip==12.13.0
```

### Q2：防火墙提示网络访问

**原因：** 路由器之间使用本地 UDP（127.0.0.1）通信。

**解决：** 点击"允许访问"，或在防火墙中添加 Python 例外。

### Q3：端口被占用

**原因：** 上次程序未正常退出。

**解决：** 任务管理器结束所有 python.exe 进程；或在配置文件中换端口。

### Q4：50 节点拓扑界面卡顿

**原因：** 大量路由表渲染开销。

**解决：** 系统已自动切换汇总模式。如仍卡顿，减少打开的标签页，或调大 `src/ui/main_window.py` 中 `timer.start(2000)` 的数值。

### Q5：如何修改心跳检测间隔？

编辑 `src/core/constants.py`：
```python
HEARTBEAT_INTERVAL = 0.8   # 心跳发送间隔（秒）
HEARTBEAT_TIMEOUT = 3      # 连续超时次数阈值
```

### Q6：如何在多台机器上分布式运行？

1. 修改配置文件中的 `listen_ip` 为各机器实际 IP
2. 确保各机器网络互通，防火墙放行对应 UDP 端口
3. 同步配置文件到所有机器
4. 分别启动

### Q7：从 GitHub 下载太慢或连不上？

**解决：**
- 使用 VPN 或代理
- 从师兄师姐处直接拷贝项目文件夹
- 或使用 GitHub 镜像站（如 kgithub.com）

---

## 11. 项目结构

```
lsa/
├── run.py                    # 主入口
├── requirements.txt          # Python 依赖
├── .gitignore
├── 实验手册.md               # 本手册
├── ui启动器.bat              # 一键安装+启动（推荐）
├── 简易启动.bat              # 快速启动
├── 启动.bat                  # 标准启动
├── 诊断.bat                  # 诊断模式
├── config/                   # 网络拓扑配置（JSON）
│   ├── network_topology.json
│   ├── topology_small_demo.json
│   ├── topology_medium_demo.json
│   ├── benchmark_topology_50.json
│   └── test_topology*.json
├── docs/
│   ├── TECHNICAL.md
│   └── USAGE.md
├── src/
│   ├── simulator.py          # 仿真主控制器
│   ├── algorithms/           # 算法模块 ★ 添加新算法在这里
│   │   ├── dijkstra.py
│   │   ├── new_algorithm.py
│   │   ├── duan_algorithm.py
│   │   ├── extended_algorithms.py
│   │   └── stepped_dijkstra.py
│   ├── core/                 # 协议与数据结构
│   │   ├── constants.py
│   │   ├── lsa.py
│   │   ├── protocol.py       # ★ 新算法注册在这里
│   │   ├── router.py
│   │   └── topology.py
│   └── ui/                   # PyQt5 界面
│       ├── main_window.py    # ★ 新算法显示名在这里
│       ├── topology_view.py
│       ├── animation_view.py
│       ├── teaching_demo.py
│       └── styles.py
└── tests/
    └── run_tests.py
```

### 系统架构

```
┌──────────────────────────────────────────┐
│           UI 层 (PyQt5)                   │
│  教学演示 │ 拓扑显示 │ 算法动画 │ 路由表  │
│  算法对比 │ 仿真控制                      │
└──────────────────┬───────────────────────┘
                   │
┌──────────────────▼───────────────────────┐
│        仿真系统层 (RoutingSimulator)       │
│  - 路由器实例管理  - 拓扑切换/故障注入     │
└──────────────────┬───────────────────────┘
                   │
┌──────────────────▼───────────────────────┐
│           路由器层 (Router)               │
│  UDP通信 │ LSA洪泛 │ 心跳检测 │ 5个线程   │
└──────────────────┬───────────────────────┘
                   │
┌──────────────────▼───────────────────────┐
│        协议与算法层                        │
│  PathCalculator ← 7种最短路径算法          │
│  TopologyDB + RoutingTable + LSADatabase  │
└──────────────────────────────────────────┘
```

---

## 参考资料

- Dijkstra, E. W. (1959). A note on two problems in connexion with graphs.
- RFC 2328 — OSPF Version 2
- RFC 2453 — RIP Version 2
- Kurose & Ross. *Computer Networking: A Top-Down Approach* (8th Edition)
- Duan, Mao, Mao, Shu, Yin (2025). Breaking the Sorting Barrier for Directed SSSP. STOC 2025.
- NetworkX: https://networkx.org/
- PyQt5: https://www.riverbankcomputing.com/static/Docs/PyQt5/

---

*本手册面向计算机网络课程的教师和学生，以及后续维护该项目的开发者。*
*项目地址：https://github.com/nurzat0300/lsa*
